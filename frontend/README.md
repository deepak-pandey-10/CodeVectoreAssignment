# Product Frontend

React frontend for the existing Express product API.

## Run

```bash
npm install
npm run dev
```

By default the app calls `http://localhost:5000/api/products`.

To point it at another backend URL, create `.env`:

```bash
VITE_API_BASE_URL=http://localhost:5000
```
